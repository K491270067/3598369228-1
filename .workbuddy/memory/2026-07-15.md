# 2026-07-15

## 增强 CK3 模组调试 prompt
- 任务：用户要增强一份 CK3 模组调试 prompt（原文件 C:\Users\15550\AppData\Roaming\Code\User\prompts\ck3-mod-dev.prompt.md 是「代码生成型」prompt，含强约束：原版为模板、严格语法）。
- 源文件：原始 prompt（生成型，含 confederation/house_bloc 系统、scope、GUI 规范）、教程 PDF《P语言：从入门到弃坑》53页（覆盖载入顺序/覆盖vs追加、事件 mtth/is_triggered_only、#注释、本地化双字节yml）。
- 产出：ck3-mod-debug.prompt.md（增强版「排查修复型」prompt），落在 mod 工作目录根。
- 关键增强点：定义调试专家角色；显式列出参考资源（原版路径、Wiki、PDF章节、crashes/error.log/game.log/data_types）；铁律（最小改动、改前先读原版）；6步工作流（Triage→Recon→Scan→对照原版→Fix→Re-check）；A/B 两类 bug 分类（崩溃/卡顿的具体 CK3 成因）；输出规范（bug 表格+改动总览+残留风险+结论）；收尾质量门 checklist。
- 注意：原 prompt 引用的 Paradox 文档在 E: 盘（E:\documents\Paradox Interactive\...），崩溃日志 ck3_20260715_122142 与当前日期一致。

## 实际调试「大梦春秋 III」崩溃/卡顿
- 崩溃签名：EXCEPTION_ACCESS_VIOLATION (C0000005) 在 ck3.exe 原生层（1.19.0.6）。error.log 尾部被刷屏：
  - `culture trigger [ Failed context switch ]` @ 原版 `common/vassal_stances/00_vassal_stances.txt` 多行（模组**未覆盖**该文件）
  - `religion_tag [ Invalid religion tag 'islam_religion' ]` ×5253、`geographical_region trigger [ Not found in database ]` ×8320、`Scoped object of type 'faith' is not valid (Faith-4294967295)` ×257（空 faith → 崩溃源）
- **根因（已验证）**：模组是中式总转换，丢弃了大量原版数据库对象（宗教 islam_religion 等、文化 somali、文化传承 heritage_*、创新、地理区域、信仰家族 rf_*、特质 fragile_bones），却**未覆盖**仍在引用它们的原版文件（vassal_stances / succession_laws / scripted_character_templates(repopulation) / script_values / lease_contracts / culture creation_names / doctrine_types）。生成角色没拿到有效 faith/culture → 空指针被解引用崩溃；海量报错刷屏 = 卡顿。
- **验证要点**：① Glob 确认模组无 `common/vassal_stances/`；② 模组 `common/religion/` 仅 lijiao/beifang/nanfang（已丢原版宗教）；③ error.log 三类签名合并 5854 行真实存在；④ 推翻误判：han32/han33 用的 xizhouli/dongzhouli/zhou **在模组里有定义**，不是崩溃源；⑤ agent 误标：`zhoutianzi.txt:780` 实为 bofu 角色开头（非"父辈9岁生子"），repopulation 模板 1594/1208 行属**原版文件**（模组无此文件）。
- **修复策略待用户拍板**：A) 补 stub 定义被删对象（重、可能让原版机制意外生效）；B) 逐个覆盖引用了丢弃对象的原版文件（正、费力）；C) 先保证所有角色有有效 faith/culture 止血（针对崩溃）。
- 产出：debug_report.md（engineering-lead 撰写，仅方案未落盘）。

## 实施修复（Option B 覆盖 + 轻量安全网）
- 用户先选 Option B，工程负责人落地第一批 4 覆盖：common/vassal_stances/00_vassal_stances.txt（删 minority 对 islam/christianity/judaism/zoroastrianism 引用）、common/laws/00_succession_laws.txt、common/script_values/01_dynamic_values.txt、common/scripted_character_templates/00_pool_repopulation_character_templates.txt。均已验证最小、引用清零、括号平衡。
- 但 Option B 救不了崩溃（角色仍拿空 faith → Faith-4294967295 解引用崩溃）；真实规模 ~480 覆盖文件，模组自家 15 文件仍引用被删宗教。工程负责人误标 zhoutianzi.txt:780=父辈9岁生子（主理人已读证伪：该行是 bofu 角色开头 father=han30）。
- 用户改选「轻量脚本保证 faith/culture」作崩溃止停。新建 common/on_action/faith_culture_safety.txt（on_action dm_faith_culture_safety + on_yearly 钩子），最小改动 common/on_action/game_start.txt（on_game_start on_actions +1 行 dm_faith_culture_safety）。默认 faith=xizhouli、culture=zhou（均模组已有；lijiao 是宗教类型非信仰，真实信仰是 xizhouli）。
- 检测惯用法：has_faith/has_culture 叶子触发器（对 NULL 返回 false、不触发 Failed context switch）+ NOT/OR 全有效对象枚举（11 faith/36 culture），零误伤不 uniform 化。
- 验证（主理人独立）：has_faith=faith:、set_character_faith=、set_culture=、on_action 内直接 effect={ 均确认原版真存在语法；括号配平、被删对象零引用。
- 待用户验证：本环境不能跑游戏，须重新加载存档后查 crashes/ 与 error.log，确认 Faith-4294967295 / Failed context switch / Invalid religion tag 归零。
- 已知局限：OR 列表为快照枚举，模组增删信仰文化须同步更新；安全网只兜底坏数据不修根因。若重载后崩溃仍在，回退 Option A（补/stub 被删对象）。
